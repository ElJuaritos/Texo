-- Texo demo seed — datos estáticos complementarios al script scripts/seed-demo.mjs
-- Ejecutar tras migraciones: supabase db reset (local) o node scripts/seed-demo.mjs (remoto)

-- Nota: usuarios auth se crean vía scripts/seed-demo.mjs con service role.
-- Este archivo reserva extensiones futuras (p. ej. datos de referencia).

SELECT 1;
