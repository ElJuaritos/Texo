# @texo/web

Next.js 15 App Router — marketplace Texo.

## Dev

```bash
# desde la raíz del monorepo
npm run dev:web
```

## Reglas

- Importar types/queries solo desde `@texo/shared`.
- No duplicar enums de dominio.
- Ver `docs/contracts/` y `docs/AGENT_SYNC.md`.
